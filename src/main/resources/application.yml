spring:
  application:
    name: app
  profiles:
    active: local
    group:
      local: local-db
      dev: dev-db
  config:
    import:
      - "classpath:config/local/application-local-db.yml"
      - "classpath:config/dev/application-dev-db.yml"